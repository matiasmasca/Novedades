<div class="container-fluid">
  <h1 align="center"><strong>Caso:</strong> <%= @notification.project.name %></h1>
  <h4 align="left"><strong>Título:</strong> <%= @notification.title %> <button type="button" class="btn btn-default">Marcar como leída</button> </h4>
  <h4 align="left"><strong>Fecha:</strong> <%= @notification.fecha %> </h4>
  <hr size="1" />
  <h4 align="left"><strong>Detalle:</strong></h4>
  <h5 align="left"><%= @notification.message %></h5>
  <hr size="1" />
  <h4 align="left">Adjuntos:</h4>

  <table  class="table table-striped">
    <thead>
      <tr>
        <th>Nro.</th>
        <th>Detalle adjunto</th>
        <th>Archivo adjunto</th>
        <th></th>
      </tr>
    </thead>

    <tbody>
      <% @attachments.each do |attachment| %>
        <tr>
          <td><%= attachment.id %></td>
          <td><%= attachment.title %></td>
          <td>
            <% if attachment.profile_image_id? %>
             <%= link_to "Descargar Adjunto", request.base_url + attachment.path, class: "btn btn-default", target: "_black", download: attachment.title  %>
            <% end %>
          </td>
          <td>
          <!-- Collap -->
          <div class="bs-example">
          <p>
            <a class="btn btn-primary collapsed" role="button" data-toggle="collapse" href="#collapse-<%= attachment.id %>" aria-expanded="false" aria-controls="collapseExample">
              Ver Archivo
            </a>
          </p>
        </div>
        <!-- /Collap -->
         </td>
        </tr>
        <!-- Collapsed -->
        <tr>
          <td>
            <div style="height: 0px;" aria-expanded="false" class="collapse" id="collapse-<%= attachment.id %>">
              <div class="well">
                <!--/show attach -->
                  <% @attachment = attachment %>
                  <%= render partial: "attachments/show", object: @attachment %>
                <!--/show attach -->
              </div>
            </div>
         </td>
       </tr>
        <!-- /Collapsed -->
      <% end %>


    </tbody>
  </table>

  <hr size="1" />

  <h4 align="left">Comentarios:</h4>

  <table class="table table-striped">
      <tbody>
          <tr>
              <td>Cliente</td>
              <td>Debo presentarme el lunes en el juzgado?</td>
              <td>día 11/11/1111</td>
          </tr>
          <tr>
              <td>Dr. Cinicius</td>
              <td>No será necesario, ya que envié un escrito para evitarlo. Saludos.</td>
              <td>día 11/11/1111</td>
          </tr>
      </tbody>
  </table>

<%= link_to 'Edit', edit_notification_path(@notification) %> |
<%= link_to 'Back', :back %>
<div>
