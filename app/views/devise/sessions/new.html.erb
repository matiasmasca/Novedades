

<div class="container-fluid" align="center">
    <p></p>
    <div class="media">
        <img class="media-object" src="http://placehold.it/140x140">
        </a>
        <div class="media-body"></div>
    </div>
    <p></p>

    <div class="authform">
      <%= form_for(resource, :as => resource_name, :url => session_path(resource_name), :html => { :role => 'form'}) do |f| %>
        <%= devise_error_messages! %>
        <div class="form-group">
          <%- if devise_mapping.registerable? %>
            <%= link_to 'Registrarse', new_registration_path(resource_name), class: 'right'  %>
          <% end -%>
          <%= f.label :email, "Usuario" %>
          <%= f.email_field :email, :autofocus => true, class: 'form-control', maxlength: "110", placeholder: "Usuario" %>
        </div>
        <div class="form-group">
          <%- if devise_mapping.recoverable? %>
            <%= link_to "Olvidé mi contraseña", new_password_path(resource_name), class: 'right' %>
          <% end -%>
          <%= f.label :password, "Clave" %>
          <%= f.password_field :password, class: 'form-control', maxlength: "110", placeholder: "Contraseña" %>
        </div>
          <%= f.submit 'Ingresar', :class => 'button right' %>
          <% if devise_mapping.rememberable? -%>
            <div class="checkbox" style="width:150px">
              <label>
                <%= f.check_box :remember_me %> Recordarme
              </label>
            </div>
          <% end -%>
      <% end %>
    </div>
